number = int(input(""))#隊伍總數
res = 0#計數

for i in range(number):#重複number次
    member = int(input(""))#輸入各隊人數
    if member % 3 == 0 and member > 0:#判斷隊伍人數是否符合
        res = res + 1
print(res)#符合標準的隊伍數量
